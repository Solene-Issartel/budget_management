<section class="topMargin">
    <div class="container">
        <div class="row">
            <div class="col-lg-12 text-center">
                <h2 class="text-uppercase section-heading">Liste des listes</h2>
                <h3 class="text-muted section-subheading">Gérez les listes : vous pouvez les modifier, les supprimer, <a href="/lists/create">en ajouter</a>.</h3>
            </div>
        </div>
        {{#if errors}}
            <div class="alert {{errors.alert}}">
                {{errors.msg}}
            </div>
        {{/if}}
        <div class="row text-center">
            <div class=" col-md-4">
                {{#each col_1 as | li |}}

                    <div class="cardList card">
                        <div class="card-body">
                            <h4 class="card-title">Liste du {{li.date_list}}</h4><a class="card-link"href="/lists/update/{{id_list}}">Modifier</a><a class="card-link" onclick="changeUrl({{id_list}})" data-toggle="modal" data-target="#DelCatModal">Supprimer</a>
                        </div>
                        
                    </div>
                {{/each}}
                
            </div>
            <div class="col-md-4">
                {{#each col_2 as | li |}}

                    <div class="cardList card">
                        <div class="card-body">
                            <h4 class="card-title">Liste du {{li.date_list}}</h4><a class="card-link"href="/lists/update/{{id_list}}">Modifier</a><a class="card-link" onclick="changeUrl({{id_list}})" data-toggle="modal" data-target="#DelCatModal">Supprimer</a>
                        </div>
                        
                    </div>
                {{/each}}
            </div>
            <div class="col-md-4">
                {{#each col_3 as | li |}}

                    <div class="cardList card">
                        <div class="card-body">
                            <h4 class="card-title">Liste du {{li.date_list}}</h4><a class="card-link"href="/lists/update/{{id_list}}">Modifier</a><a class="card-link" onclick="changeUrl({{id_list}})" data-toggle="modal" data-target="#DelCatModal">Supprimer</a>
                        </div>
                        
                    </div>
                {{/each}}
            </div>
        </div>
    </div>
    <!-- Modal -->
    <div class="modal fade" id="DelListModal" tabindex="-1" role="dialog" aria-labelledby="DelListModal" aria-hidden="true">
        <div class="modal-dialog" role="document">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title" id="DelListModal">Attention</h5>
                    <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                    </button>
                </div>
                <div class="modal-body">
                    Cette action est irréversible. Voulez-vous vraiment supprimer cette liste ?
                </div>
                <div class="modal-footer">
                    <form id="deleteForm" method="post" action="">
                        <button type="button" class="btn btn-secondary" data-dismiss="modal">Annuler</button>
                        <input type="hidden" name="_method" value="DELETE">
                        <button id="deleteList" name="delete" value="delete" type="submit" class="btn btn-danger">Supprimer</button>
                        <input type="hidden" name="_csrf" value="{{csrfToken}}">
                    </form>
                </div>
            </div>
        </div>
    </div>
</section>

<script>
    function changeUrl(id){
        $("form#deleteForm").attr("action","/categories/delete/"+id+"?_method=DELETE");
    }
</script>