<section class="topMargin">
    <div class="container">
        <div class="row">
            <div class="col-lg-12 text-center">
                <h2 class="text-uppercase section-heading">listes des utilisateurs</h2>
                <h3 class="text-muted section-subheading">Gérez les utilisateurs : vous pouvez les modifier de leurs droits, les supprimer.</h3>
            </div>
        </div>
        {{#if errors}}
            <div class="alert {{errors.alert}}">
                {{errors.msg}}
            </div>
        {{/if}}
        <div class="row text-center">
            <div class=" col-md-4">
                {{#each col_1 as | list |}}

                    <div class="cardList card">
                        <div class="card-body">
                            <h4 class="card-title"><a class="userCard card-link" href="/users/{{id}}">{{list.lastname}} {{list.firstname}}</a></h4>
                        </div>
                        
                    </div>
                {{/each}}
                
            </div>
            <div class="col-md-4">
                {{#each col_2 as | list |}}

                    <div class="cardList card">
                        <div class="card-body">
                            <h4 class="card-title"><a class="userCard card-link" href="/users/{{id}}">{{list.lastname}} {{list.firstname}}</a></h4>
                        </div>
                    </div>
                {{/each}}
            </div>
            <div class="col-md-4">
                {{#each col_3 as | list |}}

                    <div class="cardList card">
                        <div class="card-body">
                            <h4 class="card-title"><a class="userCard card-link" href="/users/{{id}}">{{list.lastname}} {{list.firstname}}</a></h4>
                        </div>
                        
                    </div>
                {{/each}}
            </div>
        </div>
    </div>
</section>

{{!-- 
<section class="topMargin">
    <div class="container">
        <div class="row">
            <div class="col-lg-12 text-center">
                <h2 class="text-uppercase section-heading">Liste des utilisateurs</h2>
                <h3 class="text-muted section-subheading">Gérez les utilisateurs : vous pouvez modifier leur droit, les supprimer.</h3>
            </div>
        </div>
        {{#if errors}}
            <div class="alert {{errors.alert}}">
                {{errors.msg}}
            </div>
        {{/if}}
        <div class="row" style="margin-top:80px;margin-bottom:100px">
            <div class="row text-center">
                <div class=" col-md-4">
                    {{#each list_1 as | list |}}
                        <div class="cardList card">
                            <div class="card-body">
                                <a class="card-link" href="/users/{{id}}">{{list.lastname}} {{list.firstname}}</a>
                            </div>
                        </div>
                    {{/each}}
                </div>
                <div class=" col-md-4">
                    {{#each list_2 as | list |}}
                        <div class="cardList card">
                            <div class="card-body">
                                <a class="card-link" href="/users/{{id}}">{{list.lastname}} {{list.firstname}}</a>
                            </div>
                        </div>
                    {{/each}}
                </div>
                <div class=" col-md-4">
                    {{#each list_3 as | list |}}
                        <div class="cardList card">
                            <div class="card-body">
                                <a class="card-link" href="/users/{{id}}">{{list.lastname}} {{list.firstname}}</a>
                            </div>
                        </div>
                    {{/each}}
                </div>
            </div>
        </div>
    </div>
</section> --}}